name: Docker CI

on: push

jobs:  
  build:
    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v1

    - name: Copy files to final destination
      run: |
        ls
        pwd

    - name: copy file
      uses: canastro/copy-file-action@master
      with:
        source: "README.md"
        target: "/opt/homer/"